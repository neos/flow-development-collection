type: dictionary
properties:
  'Default':
    type: dictionary
    required: true
    additionalProperties: FALSE
    properties:
      'backend': {type: string, format: class-name, required: true}
      'frontend': {type: string, format: class-name, required: true}
      'backendOptions': {type: dictionary}
      'persistent': {type: boolean}
additionalProperties:
  type: dictionary
  additionalProperties: FALSE
  properties:
    'backend': {type: string, format: class-name}
    'frontend': {type: string, format: class-name}
    'backendOptions': {type: dictionary}
    'persistent': {type: boolean}
